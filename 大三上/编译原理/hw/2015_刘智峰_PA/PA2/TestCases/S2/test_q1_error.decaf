class Cow {
    int height;
    int weight;

    void Init(int w, int h) {
        weight = w;
        height = h;
    }
}

class Main {
    static void main() {
    	int r;
    	bool a;
    	r=a++;
    	r=a--;
    	r=++a;
    	r=--a;

    	r=v++;
    	r=v--;
    	r=++v;
    	r=--v;
    }
}

